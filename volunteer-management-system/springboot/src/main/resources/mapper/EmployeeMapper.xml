<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.EmployeeMapper">
    <insert id="insert" parameterType="com.example.springboot.entity.Employee">
        INSERT into employee (id,username,password,role,name,sex,no,age,description,department_id)
        values (#{id},#{username},#{password},#{role},#{name},#{sex},#{no},#{age},#{description},#{departmentId})
    </insert>


    <update id="updateById" parameterType="com.example.springboot.entity.Employee">
        UPDATE employee
        set username=#{username},password=#{password},role=#{role},name=#{name},sex=#{sex},no=#{no},age=#{age},description=#{description},department_id=#{departmentId}
        where id=#{id}
    </update>

    <delete id="deleteById" parameterType="com.example.springboot.entity.Employee">
        DELETE from employee
        where id=#{id}
    </delete>


    <select id="selectAll" resultType="com.example.springboot.entity.Employee">
        SELECT * FROM employee
        <where>
            <if test="name !=null">name like concat('%',#{name},'%')</if>
        </where>
        order by id desc
    </select>
</mapper>